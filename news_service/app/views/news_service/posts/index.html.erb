<%= content_for(:title, page.page_title) %>

<h1><%= page.title %></h1>

<ul>
  <% page.posts.each do |post| %>
    <li>
      <%= link_to post.title, post_path(post.id) %>
    </li>
  <% end %>
</ul>
